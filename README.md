# Embedded Linux Portfolio

Simple, dependency-free portfolio site built from the provided resume.

## Run locally

From the repo root:

```bash
python -m http.server 8000
```

Open `http://localhost:8000` in a browser.

## Notes

- Update the social links in `index.html` where it says "Add your link".
- All visuals are local SVGs under `assets/`.
